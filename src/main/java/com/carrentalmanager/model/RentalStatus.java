package com.carrentalmanager.model;

/**
 * Enum (serve per definire delle istanze predefinite) che rappresenta lo stato di un noleggio:
 * - APERTO: il noleggio è attualmente attivo
 * - CHIUSO: il noleggio è stato completato e chiuso
 */
public enum RentalStatus {
    APERTO,
    CHIUSO
}
